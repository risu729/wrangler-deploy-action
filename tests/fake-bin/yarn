#!/usr/bin/env bash

set -euo pipefail

if [[ $# -lt 6 || $1 != "--cwd" || $3 != "run" || $4 != "-B" || $5 != "wrangler" ]]; then
	echo "Unexpected Yarn invocation: $*" >&2
	exit 1
fi

working_directory="$2"
shift 5
cd "${working_directory}"
wrangler "$@"
